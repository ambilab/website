---
import { SITE } from '@config/site';

interface SocialLink {
    name: string;
    url: string;
    icon: string;
}

const socialLinks: SocialLink[] = [
    { name: 'X (Twitter)', url: SITE.SOCIAL.X, icon: '𝕏' },
    { name: 'Threads', url: SITE.SOCIAL.THREADS, icon: '◈' },
    { name: 'Instagram', url: SITE.SOCIAL.INSTAGRAM, icon: '📷' },
    { name: 'LinkedIn', url: SITE.SOCIAL.LINKEDIN, icon: 'in' },
    { name: 'Mastodon', url: SITE.SOCIAL.MASTODON, icon: '🐘' },
    { name: 'Bluesky', url: SITE.SOCIAL.BLUESKY, icon: '🦋' },
    { name: 'GitHub', url: SITE.SOCIAL.GITHUB, icon: 'GH' },
];
---

<div class="flex flex-wrap gap-3">
    {
        socialLinks.map((link) => (
            <a
                href={link.url}
                target="_blank"
                rel="noopener noreferrer"
                class="border-border-default hover:bg-surface-hover dark:border-border-default-dark dark:hover:bg-surface-hover-dark flex h-9 w-9 items-center justify-center rounded-lg border text-sm font-medium transition-colors"
                aria-label={link.name}
                title={link.name}
            >
                {link.icon}
            </a>
        ))
    }
</div>
