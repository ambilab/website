---
/**
 * SkipLink
 *
 * Accessibility skip-to-content link for keyboard users.
 * Visually hidden until focused; allows bypassing repeated navigation.
 * WCAG 2.1 Level A: 2.4.1 Bypass Blocks.
 */
import { getTranslation } from '@i18n/translations';
import type { Locale } from '@type/locale';

interface Props {
    locale: Locale;
}

const { locale } = Astro.props as Props;
const t = getTranslation(locale);
---

<a
    href="#main-content"
    class="meta sr-only focus:not-sr-only focus:absolute focus:left-2 focus:top-2 focus:z-skip-navigation-link focus:bg-page-bg focus:px-2 focus:py-1 focus:text-text-primary focus:outline-none focus:ring-4 focus:ring-focus-ring"
>
    {t.a11y.skipNavigationLink}
</a>
